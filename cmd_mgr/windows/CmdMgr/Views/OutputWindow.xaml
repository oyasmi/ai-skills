<Window x:Class="CmdMgr.Views.OutputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Output" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SurfaceBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status bar -->
        <DockPanel Grid.Row="0" Margin="14,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <Ellipse x:Name="StatusDot" Width="8" Height="8"
                         Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                <TextBlock x:Name="StatusText" Text="Running" FontSize="12"
                           Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
            <Button Content="Copy All" Style="{StaticResource ModernButton}"
                    Click="CopyAll_Click" DockPanel.Dock="Right" HorizontalAlignment="Right"
                    Padding="10,4" FontSize="11"/>
        </DockPanel>

        <Separator Grid.Row="1"/>

        <!-- Output text – background from theme, not hardcoded white -->
        <TextBox x:Name="OutputBox" Grid.Row="2"
                 IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11"
                 Padding="12" BorderThickness="0"
                 Background="{StaticResource SurfaceBrush}"/>
    </Grid>
</Window>
