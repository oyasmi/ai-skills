interface:
  display_name: "AgentMux"
  short_description: "通过 AgentMux 委派并管理外部 CLI coding agent"
  default_prompt: "使用 $agentmux 选择合适的 harness，创建或复用外部 coding agent 实例，发送包含明确目标、相关上下文、范围边界和可验证完成标准的任务指令，耐心等待并读取输出，最后直接验证实际交付物；全程不要直接调用 tmux。"
